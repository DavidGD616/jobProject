# ADR-XXXX — <short title, a noun phrase>

**Status:** Proposed | Accepted | Rejected | Superseded by ADR-YYYY
**Date:** YYYY-MM-DD

## Context

What forces are at play? What is true right now that makes this a decision rather than an obvious default? Include constraints that feel too obvious to write down — those are exactly the ones nobody remembers in six months.

## Options

### A. <name>
Pros / cons.

### B. <name>
Pros / cons.

## Decision

One sentence, active voice. "We will X."

## Consequences

What becomes easier. What becomes harder. What is now hard to reverse, and what the escape hatch is if this turns out wrong.

## Revisit when

The concrete trigger that should reopen this — a scale threshold, a new requirement, a dependency change. Without this, ADRs calcify.
